const { Treasure } = require('./Treasure');
const { LiveVideo } = require('./LiveVideo');
const { Sign } = require('./Sign');
const { GuessCar } = require('./GuessCar');
const { Daoju } = require('./Daoju');
const { FunWeekly } = require('./FunWeekly');
const { Club } = require('./Club');

module.exports = {
    Treasure,
    LiveVideo,
    Sign,
    GuessCar,
    Daoju,
    FunWeekly,
    Club
}